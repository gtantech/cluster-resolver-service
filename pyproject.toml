[project]
name = "cluster-resolver-service"
version = "0.1.0"
description = "A microservice that resolves cluster labels for timeseries data"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []
